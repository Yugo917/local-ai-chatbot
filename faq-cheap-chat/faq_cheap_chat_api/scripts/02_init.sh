# #########################
# Init Project
# #########################

# Initialize a new Poetry project in the current directory.
# This will create a `pyproject.toml` file, which serves as the configuration file
# for project dependencies, metadata, and build system requirements.
poetry init